The test suite validates user autre ideal for regression test use, when new releases are published we can have some confidence and evidence that system is still working hentication with valid and invalid credentials, shopping cart operations including adding and removing items, the multi-step checkout process with required field validation, and user navigation between pages. The tests can identify authentication failures, missing server-side validation, cart data persistence issues, session management problems, and UI interaction failures.

These tests were chosen in order to tackle potential login authentication risks product might have. Also testing end-to-end flow of how real user would use the website tests critical findings that could prevent user to make purchases, and eventually business would be impacted. I think these test cases aas expected.

Tests and resources were created using simple but maintainabled structure:

tests/
├─ test_suite.robot
resources/
├─ keywords.robot
├─ variables.robot